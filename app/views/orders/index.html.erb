<div class="container">
  <div class="row">
    <% @orders.each do |order| %>
      <div class="col-sm">
        <h1 class= "order-title">
          Order # <%= order.id %>:
          <p id= "order-prod-name">
          <%= link_to order.product&.name, order %>
        </p>
        </h1>
        <p><%= order.product&.description %></p>
        <strong>€ <%= order.product&.price %></strong>
      </div><!-- /col -->
    <% end %>
  </div>
</div>
