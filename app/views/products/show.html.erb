<div class= "show-div container">

  <div class= "show-content">
    <p id="notice"><%= notice %></p>

    <p>
      <strong>Name:</strong>
      <%= @product.name %>
    </p>

    <p>
      <strong>Description:</strong>
      <%= @product.description %>
    </p>

    <p>
      <strong>Image url:</strong>
      <%= @product.image_url %>
    </p>

    <p>
      <strong>Colour:</strong>
      <%= @product.colour %>
    </p>

    <p>
      <strong>Price (€):</strong>
      <%= @product.price %>
    </p>

    <p> <%= link_to 'Edit', edit_product_path(@product), :class=> "td-btn" %> |
    <%= link_to 'Back', products_path(@product), :class=> "td-btn" %> </p>
  </div>
</div>
